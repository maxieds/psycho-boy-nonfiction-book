python3 -c "import socket; s = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM); s.bind('/path/to/destination/file.sock')"

python3 -c "import socket; s = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM); s.bind('/path/to/destination/file.sock')"

nc -U -l /path/to/destination/file.sock &

tar -cf - -C /path/to/source . | tar -xf - -C /path/to/destination

cp: /var/run/cupsd is a socket (not copied).
cp: /var/run/displaypolicyd/state is a socket (not copied).
cp: /var/run/filesystemui.socket is a socket (not copied).
cp: /var/run/mDNSResponder is a socket (not copied).
cp: /var/run/portmap.socket is a socket (not copied).
cp: /var/run/pppconfd is a socket (not copied).
cp: /var/run/syslog is a socket (not copied).
cp: /var/run/systemkeychaincheck.socket is a socket (not copied).
cp: /var/run/usbmuxd is a socket (not copied).
cp: /var/run/vpncontrol.sock is a socket (not copied).
(base) Drs-MacBook-Pro:porche911 maxieds$ vim do
docs/       dotandroid/ 
(base) Drs-MacBook-Pro:porche911 maxieds$ vim 
.DS_Store                                                               docs/
Desktop-obama-images/                                                   dotandroid/
Jan6Nov7Porche911OnJunteenth-Screen Shot 2026-06-06 at 11.13.52 AM.png  mac-terminal-logs/









